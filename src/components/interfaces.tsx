import { ReactNode } from 'react';

export interface Navbar {

}

export interface PageFrame {

}

export interface PageFrameProps {
    children?: ReactNode;
}

export interface hm_firstFrameProps {
    children?: ReactNode;
}
